version: 2.1

jobs:
  build:
    docker:
      - image: cimg/openjdk:17.0
    steps:
      - checkout
      - run:
          name: Run Maven tests
          command: mvn clean test

workflows:
  build_and_test:
    jobs:
      - build